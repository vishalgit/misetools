general {
    colors = true
}
order += "cpu_usage"
order += "memory"
order += "tztime local"
cpu_usage {
    format = "CPU: %usage"
}
memory {
    format = "Mem: %used / %total"
}
tztime local {
    format = "%Y-%m-%d %H:%M:%S"
}

